---
import { withBase } from '../utils/paths';

interface Props {
  name: string;
  class?: string;
}

const { name, class: className = 'icon' } = Astro.props;
---

<svg class={className} aria-hidden="true">
  <use href={`${withBase('icons/ocho-icons.svg')}#${name}`}></use>
</svg>
