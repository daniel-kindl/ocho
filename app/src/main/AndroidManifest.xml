<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      A session must keep exact time with the screen off. Foreground status stops
      the process being killed; WAKE_LOCK stops the CPU sleeping, which is what
      would otherwise stall the engine's delay() calls and lose interval beeps.
      Neither alone is sufficient.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Android 13+. If denied, the session still runs correctly and only the
         ongoing notification is hidden. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".OchoApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ocho">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          specialUse rather than mediaPlayback: this is a timer, not a media app.
          The honest declaration costs nothing for a sideloaded build, and claiming
          media playback would invite media-specific system behaviour we do not want.
        -->
        <service
            android:name=".data.session.SessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Interval workout timer keeping exact time and emitting audio cues while backgrounded or with the screen off." />
        </service>

    </application>

</manifest>
